<div class="container w-50">
<h2 class="title-form">Edit your profile</h2>

<%= simple_form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put }) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :email, required: true, autofocus: true %>

    <% if devise_mapping.confirmable? && resource.pending_reconfirmation? %>
      <p>Confirm your email: <%= resource.unconfirmed_email %></p>
    <% end %>

    <%= f.input :password,
                hint: "Leave it blank if you don't want to change it.",
                required: false,
                input_html: { autocomplete: "new-password" } %>
    <%= f.input :password_confirmation,
                required: false,
                input_html: { autocomplete: "new-password" } %>
    <%= f.input :current_password,
                hint: "Insert your current password to confirm changes.",
                required: true,
                input_html: { autocomplete: "current-password" } %>
    <%= f.input :photo,
                hint: "Have a better selfie?",
                required: true %>
 </div>

  <div class="form-actions btn btn-success">
    <%= f.button :submit, "Update" %>
  </div>
<% end %>
  <div class="form-actions">
    <%= link_to "Cancel my account", registration_path(resource_name), class:"btn btn-link", data: { confirm: "Are you sure?" }, method: :delete %>
  </div>
  <div class="container">
    <%= link_to 'Back', root_path, class: "btn btn-dark align-self-end" %>
  </div>
</div>
