<h2>Ships for Sale</h2>

<div class="container">
  <div class="row mt-5">
    <% @vehicles.each do |vehicle| %>
      <div class="col-12 col-md-6">
        <div class="">
          <% if vehicle.photos.attached? %>
            <%= cl_image_tag vehicle.photos[0].key, class: "card-img-top" %>
          <% else %>
            <%= image_tag 'placeholder.jpg', class: "card-img-top" %>
          <% end %>
          <div class="card border-secondary mb-3">
            <div class="card-header bg-transparent border-secondary pt-3">
              <h5><%= link_to vehicle.name, vehicle_path(vehicle) %></h5>
            </div>
            <div class="card-body bg-transparent card-size bg-opacity-50 overflow-auto">
              <p class="mb-1"><%= vehicle.description %></p>
            </div>
            <div class="card-footer bg-transparent bg-opacity-50 d-flex gap-2 border-top-0">
              <%= link_to 'View Details', vehicle_path(vehicle), class: "btn-glow btn btn-outline-light align-self-end" %>
              <%= link_to 'Delete Vehicle', order_path(vehicle), method: 'DELETE', data: { confirm: 'Are you sure?' }, class: "btn-glow-danger btn btn-danger align-self-end" %>
            </div>
          </div>
        </div>
      </div>
    <% end %>
  </div>
</div>



<h2>My orders</h2>
<div class="container">
  <div class="row mt-5">
    <% @orders.each do |order| %>
      <div class="col-12 col-md-6">
        <div class="">
          <% if order.vehicle.photos.attached? %>
            <%= cl_image_tag order.vehicle.photos[0].key, class: "card-img-top" %>
          <% else %>
            <%= image_tag 'placeholder.jpg', class: "card-img-top" %>
          <% end %>
          <div class="card border-secondary mb-3">
            <div class="card-header bg-transparent border-secondary pt-3">
              <h5><%= link_to order.vehicle.name, vehicle_path(order.vehicle) %></h5>
            </div>
            <div class="card-body bg-transparent card-size bg-opacity-50 overflow-auto">
              <p class="mb-1"><%= order.vehicle.description %></p>
            </div>
            <div class="card-footer bg-transparent bg-opacity-50 d-flex gap-2 border-top-0">
              <%= link_to 'View Details', vehicle_path(order.vehicle), class: "btn-glow btn btn-outline-light align-self-end" %>
              <%= link_to 'Delete Vehicle', order_path(order), method: 'DELETE', data: { confirm: 'Are you sure?' }, class: "btn-glow-danger btn btn-danger align-self-end" %>
            </div>
          </div>
        </div>
      </div>
    <% end %>
  </div>
</div>
